// page functions for user experience so users don't hit a page that does not exist
import React from 'react';

const NoMatch = () => {
    return (
        <div>
            <h1>Oops! We couldn't find that page!</h1>
        </div>
    );
};

export default NoMatch;